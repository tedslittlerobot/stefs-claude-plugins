---
name: hello
description: Sanity-check that the utils plugin is installed and loading correctly.
---

# Hello from Utils

Confirm the plugin is wired up correctly, then report back:

1. State that the `utils` plugin is loaded.
2. Run `claude plugin details utils` and summarise which components
   (commands, skills, agents, hooks) it currently provides.
3. Keep the reply to a few lines — this is a smoke test, not a report.
